name: generate-books
on:
  push:
    branches:
      - master
jobs:
  generate-books:
    name: generate-books
    runs-on: ubuntu-latest
    steps:
      - run: pwd
      - run: ls -a
      - run: ls -a ../
      - uses: actions/checkout@v4
      - run: pwd
      - run: ls -a
      - run: ls -a ../